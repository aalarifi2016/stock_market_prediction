version: "3.8"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./stock_prediction:/home/node/app
    ports:
      - 8000:8000
    env_file:
      - ./.env
